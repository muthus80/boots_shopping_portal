## Orientation — pre-selected context
_The Context Engineer has already selected what you need for this task. Use the essentials below directly._

### Essentials (inline — use these directly)

#### frontend_dev output
_Most recent frontend_dev output (authoritative prior phase handoff) containing existing frontend code structure and file conflicts to build upon._

{
  "_merge_metadata": {
    "file_conflicts": [
      {
        "also_by": "T-015",
        "claimed_by": "T-013",
        "path": "frontend/src/api/products.ts"
      },
      {
        "also_by": "T-015",
        "claimed_by": "T-011",
        "path": "frontend/src/pages/ProductListPage.tsx"
      },
      {
        "also_by": "T-023",
        "claimed_by": "T-013",
        "path": "frontend/sr

#### frontend_dev output
_Additional frontend_dev output from prior sprint providing context on existing implementation._

{
  "_merge_metadata": {
    "file_conflicts": [
      {
        "also_by": "T-015",
        "claimed_by": "T-013",
        "path": "frontend/src/api/products.ts"
      },
      {
        "also_by": "T-015",
        "claimed_by": "T-011",
        "path": "frontend/src/pages/ProductListPage.tsx"
      },
      {
        "also_by": "T-023",
        "claimed_by": "T-013",
        "path": "frontend/sr

#### frontend_dev output
_Frontend_dev output with file list and environment variables relevant to sprint 3 implementation._

{
  "api_base_url": "/api/v1",
  "build_command": "npm run build",
  "changes_made": [
    "SDK session generated/modified 22 impl + 3 test files in boots-shopping-app/frontend"
  ],
  "dev_command": "npm run dev",
  "environment_variables": [
    {
      "description": "Backend API base URL",
      "name": "VITE_API_URL",
      "required": true
    }
  ],
  "files": [
    {
      "content": "impo

#### ADR-003 — Stripe PaymentIntents API with Stripe Elements for PCI-DSS Compliance
_ADR-003 defines Stripe PaymentIntents + Elements integration pattern directly required for checkout tasks T-027/T-030/T-031._

{
  "adr_id": "ADR-003",
  "alternatives": [
    "Stripe Checkout (redirect) \u2014 rejected because it takes the user off-site, breaking the 5-step checkout UX requirement",
    "Braintree \u2014 rejected because Stripe is the only provider explicitly referenced in the NFRs and user preference",
    "Manual card collection + server-side Stripe charge \u2014 rejected because it places card data on application servers, violating PCI-DSS"
  ],
  "consequences": [
    "Application servers never see raw card data \u2014 achieves PCI-DSS SAQ A compliance",
    "PaymentIntent server-side creation ensures the amount is set by the backend, preventing client-side tampering",
    "Two API calls required for checkout (create PaymentIntent + confirm), but both fit within the 5-step UX constraint",
    "Stripe webhook handling should be added post-MVP for reliable payment status updates"
  ],
  "context": "The critical NFR requires PCI-DSS compliance ensuring no cardholder data is stored on application servers. The checkout must be completable in 5 steps or fewer (US-011). We need a payment integration that handles card data entirely within the Stripe iframe while giving the backend control over order amounts.",
  "decision": "Use Stripe PaymentIntents API on the backend to create server-authorised payment intents with the correct order total, and Stripe Elements (CardElement React component) on the frontend for card capture. The frontend confirms the payment directly with Stripe using the client_secret; the backend then verifies payment_intent status before creating the order record.",
  "related_requirements": [
    "US-011",
    "NFR-security-pci"
  ],
  "status": "accepted",
  "title": "Stripe PaymentIntents API with Stripe Elements for PCI-DSS Compliance"
}

#### ADR-005 — React 18 SPA with React Query for Server State Management
_ADR-005 defines React Query, Axios, React Hook Form, and Tailwind CSS patterns that govern all frontend implementation decisions._

{
  "adr_id": "ADR-005",
  "alternatives": [
    "Next.js \u2014 considered for SSR performance benefits but rejected because the user explicitly specified react (not next.js) and SSR adds deployment complexity beyond Docker Compose",
    "Redux Toolkit \u2014 rejected in favour of React Query which handles server state, caching, and loading/error states more ergonomically with less boilerplate"
  ],
  "consequences": [
    "React Query provides built-in loading/error/empty state management satisfying US-014 with minimal custom code",
    "SPA routing via React Router v6 enables fast client-side navigation",
    "No SSR means first-paint depends on CDN-cached static JS bundle; CDN delivery is required to meet LCP 2.5s NFR",
    "Stripe Elements integrates cleanly as a React component within the checkout flow"
  ],
  "context": "The frontend needs to satisfy US-014 (loading, empty, error states), US-015 (keyboard accessibility), and perform well enough to meet the LCP 2.5s NFR. The user specified React as the frontend framework. A SPA architecture serves the dynamic filtering (US-005) and cart state updates (US-010) without full page reloads.",
  "decision": "Use React 18 SPA with React Query (TanStack Query) for server state, Axios as the HTTP client, and React Hook Form for all form handling. Tailwind CSS for styling. All forms implement ARIA labels; interactive components use semantic HTML (button, input, nav) for keyboard accessibility.",
  "related_requirements": [
    "US-014",
    "US-015",
    "NFR-performance-lcp",
    "NFR-accessibility"
  ],
  "status": "accepted",
  "title": "React 18 SPA with React Query for Server State Management"
}

#### ADR-002 — JWT with Refresh Token Rotation for Session Management
_ADR-002 defines JWT/refresh token strategy and httpOnly cookie handling required for authenticated checkout and order history flows._

{
  "adr_id": "ADR-002",
  "alternatives": [
    "Session cookies with server-side session store \u2014 rejected because it requires Redis or DB session store and is less suitable for a SPA architecture",
    "Long-lived JWTs without refresh \u2014 rejected because they cannot be revoked on logout, violating security requirements",
    "OAuth2 provider (e.g., Auth0) \u2014 rejected because the user specified auth: none (no third-party auth provider)"
  ],
  "consequences": [
    "Access tokens are stateless and verifiable without DB lookup on every request",
    "Logout and token revocation are supported via the revoked flag in refresh_tokens table",
    "httpOnly cookie for refresh token prevents XSS access to the long-lived token",
    "Token refresh adds one extra API call when access token expires, but this is transparent to the user"
  ],
  "context": "US-013 requires session persistence across page refreshes and new tabs, with graceful redirect to login on expiry. The user preference specifies no dedicated auth service (auth: none in preferences means no third-party auth provider). We need a stateless auth mechanism that supports the browser's multi-tab scenario while allowing token revocation for logout. Storing hashed refresh tokens in PostgreSQL provides revocability without a separate Redis cache.",
  "decision": "Issue short-lived (30-minute) JWT access tokens and long-lived (7-day) refresh tokens. Store a SHA-256 hash of the refresh token in the refresh_tokens table with a revoked flag. The React frontend stores the access token in memory and the refresh token in an httpOnly cookie. On access token expiry, silently call POST /api/v1/auth/refresh before retrying the original request.",
  "related_requirements": [
    "US-002",
    "US-013",
    "NFR-security-password-hashing"
  ],
  "status": "accepted",
  "title": "JWT with Refresh Token Rotation for Session Management"
}

#### Page: Checkout
_Checkout page UI design spec directly required for implementing T-027/T-030/T-031 checkout flow tasks._

{
  "auth_required": false,
  "components": [
    "SiteHeader",
    "Footer",
    "CheckoutForm",
    "Button",
    "Input",
    "Spinner",
    "Alert"
  ],
  "description": "Multi-step checkout flow including shipping, payment via Stripe Elements, and confirmation.",
  "layout": "MinimalLayout",
  "name": "Checkout",
  "related_stories": [
    "US-011",
    "US-012",
    "US-014",
    "US-015"
  ],
  "responsive_notes": "Single column centered layout. Stripe Elements card input must remain responsive to container width.",
  "route": "/checkout"
}

#### Page: OrderHistory
_OrderHistory page UI design spec required for T-032 order history frontend task._

{
  "auth_required": true,
  "components": [
    "SiteHeader",
    "Footer",
    "OrderCard",
    "Spinner",
    "Alert"
  ],
  "description": "List of past orders for authenticated users.",
  "layout": "MainLayout",
  "name": "OrderHistory",
  "related_stories": [
    "US-003",
    "US-012",
    "US-013",
    "US-014",
    "US-015"
  ],
  "responsive_notes": "Order list stacks vertically. Status badges align right on desktop, bottom left on mobile.",
  "route": "/account/orders"
}

#### Page: ProductDetail
_ProductDetail page UI design spec required for T-033 reviews UI task._

{
  "auth_required": false,
  "components": [
    "SiteHeader",
    "Footer",
    "Button",
    "ReviewList",
    "Spinner",
    "Alert"
  ],
  "description": "Detailed view of a single product, including image gallery, variants, and reviews.",
  "layout": "MainLayout",
  "name": "ProductDetail",
  "related_stories": [
    "US-007",
    "US-008",
    "US-009",
    "US-012",
    "US-014",
    "US-015"
  ],
  "responsive_notes": "Images and details are stacked on mobile, side-by-side on desktop. Reviews section is full width below.",
  "route": "/products/:id"
}

#### Page: Cart
_Cart page UI design spec needed as checkout flow starts from cart (T-027/T-030)._

{
  "auth_required": false,
  "components": [
    "SiteHeader",
    "Footer",
    "CartItem",
    "Button",
    "Alert"
  ],
  "description": "Shopping cart review page showing items, quantities, and subtotal.",
  "layout": "MainLayout",
  "name": "Cart",
  "related_stories": [
    "US-010",
    "US-012",
    "US-014",
    "US-015"
  ],
  "responsive_notes": "Cart items display as compact rows on mobile. Order summary sticks to bottom on mobile, right side on desktop.",
  "route": "/cart"
}

#### API: POST /api/v1/checkout/confirm
_API contract for POST /api/v1/checkout/confirm directly required for checkout confirmation task T-031._

{
  "auth_required": false,
  "description": "Verifies Stripe payment status, creates order record, clears cart, triggers order confirmation email. Handles both guest and authenticated checkout.",
  "idempotent": false,
  "method": "POST",
  "pagination": false,
  "path": "/api/v1/checkout/confirm",
  "query_params": [],
  "rate_limit": "10/minute",
  "related_stories": [
    "US-011"
  ],
  "request_body": {
    "payment_intent_id": "string"
  },
  "required_role": null,
  "response_schema": {
    "items": [
      {
        "color": "string",
        "product_name": "string",
        "quantity": "integer",
        "size": "string",
        "unit_price": "number"
      }
    ],
    "order_id": "string (UUID)",
    "order_number": "string",
    "shipping_address": {
      "city": "string",
      "line1": "string",
      "postal_code": "string",
      "state": "string"
    },
    "total_amount": "number"
  },
  "status_codes": {
    "201": "Order created successfully",
    "400": "Payment not succeeded or cart empty",
    "502": "Stripe verification error"
  },
  "summary": "Confirm payment, create order, send confirmation email"
}

#### API: POST /api/v1/checkout/payment-intent
_API contract for POST /api/v1/checkout/payment-intent required for Stripe payment intent creation in T-030._

{
  "auth_required": false,
  "description": "Fetches cart total, creates a Stripe PaymentIntent server-side. Returns client_secret for Stripe Elements. No card data touches application servers.",
  "idempotent": false,
  "method": "POST",
  "pagination": false,
  "path": "/api/v1/checkout/payment-intent",
  "query_params": [],
  "rate_limit": "10/minute",
  "related_stories": [
    "US-011"
  ],
  "request_body": {
    "guest_email": "string (required if not authenticated)",
    "shipping_address": {
      "city": "string",
      "country": "string",
      "line1": "string",
      "postal_code": "string",
      "state": "string"
    },
    "shipping_name": "string"
  },
  "required_role": null,
  "response_schema": {
    "amount": "integer (cents)",
    "client_secret": "string",
    "currency": "string",
    "payment_intent_id": "string"
  },
  "status_codes": {
    "200": "PaymentIntent created",
    "400": "Cart is empty or invalid",
    "502": "Stripe API error"
  },
  "summary": "Create a Stripe PaymentIntent for the current cart"
}

#### API: GET /api/v1/account/orders
_API contract for GET /api/v1/account/orders required for T-032 order history page implementation._

{
  "auth_required": true,
  "description": "Returns paginated list of past orders for the authenticated user. Returns empty array with message if no orders placed.",
  "idempotent": true,
  "method": "GET",
  "pagination": true,
  "path": "/api/v1/account/orders",
  "query_params": [
    {
      "description": "Page number (default 1)",
      "name": "page",
      "required": false,
      "type": "integer"
    },
    {
      "description": "Items per page (default 10)",
      "name": "per_page",
      "required": false,
      "type": "integer"
    }
  ],
  "rate_limit": null,
  "related_stories": [
    "US-003"
  ],
  "request_body": null,
  "required_role": "member",
  "response_schema": {
    "message": "string (present when orders is empty)",
    "orders": [
      {
        "created_at": "string (ISO 8601)",
        "id": "string (UUID)",
        "order_number": "string",
        "status": "string",
        "total_amount": "number"
      }
    ],
    "total": "integer"
  },
  "status_codes": {
    "200": "Orders returned",
    "401": "Not authenticated"
  },
  "summary": "Get authenticated user order history"
}

#### API: POST /api/v1/products/{product_id}/reviews
_API contract for POST /api/v1/products/{product_id}/reviews required for T-033 review submission task._

{
  "auth_required": true,
  "description": "Creates a review. Verifies that the authenticated user has purchased the product via order_id join. Returns 403 if no matching purchase found.",
  "idempotent": false,
  "method": "POST",
  "pagination": false,
  "path": "/api/v1/products/{product_id}/reviews",
  "query_params": [],
  "rate_limit": "5/hour",
  "related_stories": [
    "US-008"
  ],
  "request_body": {
    "rating": "integer (1-5)",
    "review_text": "string"
  },
  "required_role": "member",
  "response_schema": {
    "created_at": "string (ISO 8601)",
    "id": "string (UUID)",
    "rating": "integer",
    "review_text": "string"
  },
  "status_codes": {
    "201": "Review created",
    "401": "Not authenticated",
    "403": "User has not purchased this product",
    "422": "Validation error"
  },
  "summary": "Submit a purchase-verified product review"
}

#### API: GET /api/v1/products/{product_id}/reviews
_API contract for GET /api/v1/products/{product_id}/reviews required for T-033 reviews display task._

{
  "auth_required": false,
  "description": "Returns paginated list of reviews with average star rating for the product.",
  "idempotent": true,
  "method": "GET",
  "pagination": true,
  "path": "/api/v1/products/{product_id}/reviews",
  "query_params": [
    {
      "description": "Page number (default 1)",
      "name": "page",
      "required": false,
      "type": "integer"
    },
    {
      "description": "Items per page (default 10)",
      "name": "per_page",
      "required": false,
      "type": "integer"
    }
  ],
  "rate_limit": null,
  "related_stories": [
    "US-008"
  ],
  "request_body": null,
  "required_role": null,
  "response_schema": {
    "average_rating": "number",
    "reviews": [
      {
        "created_at": "string (ISO 8601)",
        "id": "string (UUID)",
        "rating": "integer",
        "review_text": "string"
      }
    ],
    "total_reviews": "integer"
  },
  "status_codes": {
    "200": "Reviews returned"
  },
  "summary": "List reviews for a product"
}

#### API: GET /api/v1/cart
_API contract for GET /api/v1/cart required for checkout flow to read cart contents in T-027._

{
  "auth_required": false,
  "description": "Returns all items in the current cart (guest session or authenticated user), with per-item details and subtotal.",
  "idempotent": true,
  "method": "GET",
  "pagination": false,
  "path": "/api/v1/cart",
  "query_params": [],
  "rate_limit": null,
  "related_stories": [
    "US-010"
  ],
  "request_body": null,
  "required_role": null,
  "response_schema": {
    "cart_id": "string (UUID)",
    "item_count": "integer",
    "items": [
      {
        "color": "string",
        "id": "string (UUID)",
        "price": "number",
        "primary_image": "string (URL)",
        "product_name": "string",
        "product_variant_id": "string (UUID)",
        "quantity": "integer",
        "size": "string"
      }
    ],
    "subtotal": "number"
  },
  "status_codes": {
    "200": "Cart returned (empty cart returns items: [])"
  },
  "summary": "Get current cart contents"
}

#### Technology Stack
_Technology stack confirms React 18, Stripe, and other dependencies relevant to sprint 3 frontend tasks._

Tech stack: backend=FastAPI 0.111; database=PostgreSQL 16; orm=SQLAlchemy 2.0; frontend=React 18; language=Python 3.12; hosting=Docker Compose 2.27; payment=Stripe latest; web_server=Nginx 1.25; migrations=Alembic 1.13

#### Data Model: Order
_Order data model needed to understand response shape for checkout confirmation and order history pages._

Completed purchase order for guest or authenticated user

#### Data Model: OrderItem
_OrderItem data model needed for rendering order line items in T-032 order history._

Individual line item in an order with captured unit price

#### Data Model: Review
_Review data model needed to understand request/response shape for T-033 review submission._

Purchase-verified product review linking product, user, and verifying order

#### Non-Functional Requirements (6)
_Non-functional requirements including accessibility (WCAG 2.1 AA) and performance directly govern T-034 accessibility task._

[
  {
    "category": "performance",
    "measurable_target": "",
    "priority": "high",
    "requirement": "All pages, especially product listing and product detail pages, must achieve a Largest Contentful Paint (LCP) of 2.5 seconds or less."
  },
  {
    "category": "security",
    "measurable_target": "",
    "priority": "critical",
    "requirement": "The application must use a PCI-compliant payment gateway (e.g., Stripe) to process payments, ensuring no sensitive cardholder data is ever stored on the application servers."
  },
  {
    "category": "security",
    "measurable_target": "",
    "priority": "critical",
    "requirement": "User passwords must be securely hashed using a modern algorithm (e.g., bcrypt) before being stored in the database."
  },
  {
    "category": "accessibility",
    "measurable_target": "",
    "priority": "high",
    "requirement": "The entire web application must meet Web Content Accessibility Guidelines (WCAG) 2.1 Level AA standards."
  },
  {
    "category": "reliability",
    "measurable_target": "",
    "priority": "high",
    "requirement": "The application must maintain 99.9% uptime during business hours."
  },
  {
    "category": "usability",
    "measurable_target": "",
    "priority": "medium",
    "requirement": "The checkout process, from cart to confirmation, must be completable in 5 steps or fewer."
  }
]

#### qa_engineer output
_QA engineer identified frontend registration/auth bug with 'Not Found' alert that may affect sprint 3 auth-dependent flows._

{
  "agent_observations": [
    {
      "assigned_role": "frontend_dev",
      "failing_assertion": "expect(successHeading.or(homeHeading)).toBeVisible \u2014 page shows `alert: Not Found` before form submission",
      "root_cause_hypothesis": "The RegisterPage shows a `role=\"alert\"` with \"Not Found\" before the form is submitted. This indicates the frontend AuthContext or a mounted component 

#### e2e_runner output
_E2E runner results showing 8 failures including registration redirect issue relevant to fixing auth flows in sprint 3._

{
  "failed": 8,
  "failures": [
    {
      "error": "Error: expect(page).toHaveURL(expected) failed\n\nExpected: \"http://localhost:56257/\"\nReceived: \"http://localhost:56257/register\"\nTimeout:  15000ms\n\nCall log:\n  - Expect \"toHaveURL\" with timeout 15000ms\n    34 \u00d7 unexpected value \"http://localhost:56257/register\"\n\n\n  21 |     // \"Account Created!\" heading is transient. A

#### Architecture — full document
_Full architecture document provides overall system context for implementation decisions across sprint 3 tasks._

Architecture: pattern=modular_monolith. 6 components, 17 endpoints, 10 data models, 5 ADRs, 7 diagrams.

#### Component: StripeGateway
_Stripe Gateway component description defines how frontend integrates with Stripe Elements for PCI-compliant card capture in T-030._

{
  "communication_protocol": "HTTPS REST",
  "dependencies": [],
  "description": "Stripe payment processing integration. FastAPI creates PaymentIntents server-side; the React frontend uses Stripe Elements for PCI-compliant card capture. No card data touches application servers.",
  "name": "StripeGateway",
  "port": null,
  "responsibilities": [
    "Create PaymentIntents server-side with order total and currency",
    "Verify payment success before creating order records",
    "Provide client_secret to frontend for Stripe Elements card capture"
  ],
  "type": "gateway"
}

#### Component: ReactFrontend
_ReactFrontend component description confirms SPA architecture, client-side routing, and accessibility requirements._

React 18 single-page application served as static files via Nginx. Handles all UI rendering, client-side routing, form validation, cart state, and WCAG 2.1 AA accessibility.

#### Sprint Plan — full
_Sprint plan overview confirms sprint 3 stories and overall project timeline context._

Sprint Plan: 3 sprints, 6 weeks, 186 story points

### Also available (Read a specific section on demand only if the essentials above don't cover it)
- Sprint 3 — Deliver the complete guest checkout flow with Stripe payment
- Sprint 3 — Deliver the complete guest checkout flow with Stripe payment
- Development Guidelines

> The `docs/` folder in your workspace mirrors the upstream phase outputs (PRD, sprint plan, architecture decision doc). The structured essentials you need are inline above. **Do NOT bulk-read the `docs/` folder** — open at most one specific section, and only if something inline is genuinely missing.

# PROJECT INTENT (standing context — applies to every agent)

The charter below states the project's high-level intent and invariants. Honour it above task-specific guidance, except where a human gate decision (approve/revise/reject) directs otherwise.

# Project Intent Charter

> The single source of the project's **high-level intent**. It is injected into
> every applicable pipeline agent's context, and it is the first thing a
> maintainer reads before making any low-level fix.
>
> **Why this file exists:** recurring fixes were being re-typed into each
> agent's prompt one symptom at a time, because the project's intent lived only
> in the maintainer's head and scattered docs — not anywhere the agents could
> see it. When a fix is really a *principle*, it belongs here (stated once, for
> all agents), not in N separate prompts.
>
> **Version:** v1 — Session 71 (2026-06-18). Edits here are intentional and
> propagate to every agent on the next run.

---

## What this system is

An autonomous, multi-agent SDLC platform. The agents **identified during
project onboarding** (phase selection + agent selection) carry the work from
requirements through delivery, end to end. Humans **oversee and steer** at
gates — they do not write the software themselves.

---

## Operating principles (invariants — apply to every applicable agent)

1. **Human-in-the-loop at every assigned gate.** Each phase has an assigned
   gate. The pipeline pauses there for a human to **approve / revise / reject**.
   No agent advances past a gate, merges, or deploys on its own. Revise/reject
   feedback (free-text + field-level edits) is authoritative and must be
   addressed on the next pass.

2. **Clarify before you build.** Before doing its job, every applicable agent
   surfaces **genuine ambiguities** in its inputs as clarification questions to
   the human and waits for answers. If nothing is genuinely ambiguous, it
   proceeds without inventing questions — clarification is for real gaps, not a
   forced checkpoint. _(Today this is wired for the Product Owner only; it is
   intended for all applicable agents.)_

3. **Honor the user's tech stack; choose it only when unspecified.** If the user
   provided a tech stack, defer to it — never silently override it or hardcode a
   different framework/library. If the user did **not** provide one, select the
   best fit for the requirements and **record the rationale** so a human can
   review the choice at the gate.

4. **Stay within assigned scope.** Work only the tasks/stories assigned for the
   current sprint/phase. Stubs and not-yet-built dependencies are **expected** —
   verify reachability, do not over-implement beyond scope.

5. **Verify against running behavior, not just generated text.** Prefer evidence
   from a live, executed system (tests run, endpoints exercised) over claims
   about code that was only written.

6. **Never fabricate.** Do not invent test results, evidence, or content. When
   something required is missing or could not be produced, say so explicitly and
   mark the gap for the human reviewer.

7. **Protect existing work (brownfield).** Never corrupt the customer's
   repository. Keep changes to the smallest blast radius that satisfies the
   task. "Done" means the relevant tests actually pass.

8. **Classic and agentic are distinct build shapes.** A classic application and
   an agentic-framework application differ in scaffold, dependencies, and
   verification needs. Build for the shape the project declares.

> All eight principles are confirmed by the project owner (Session 71).
> Principles 1–3 were stated directly; 4–8 were surfaced from repeated
> corrections across prior sessions and ratified.

---

## How a fix is classified (maintainer discipline)

When a problem surfaces during a run, route the fix to its real home instead of
patching one prompt:

| The fix is really… | Goes to |
|---|---|
| a tech-stack-specific detail | the **stack profile** (multi-stack plan), not a prompt |
| a cross-cutting principle | **this charter** — one edit, all agents |
| a genuinely agent-local instruction | that **agent's prompt** |

A correction a human makes at a gate, if it reflects a principle, should be
promoted into this charter so the next run does not repeat it.

---

You are an elite Frontend Developer Agent for the Elite AI Pod system.
You generate production-ready frontend source code scoped to your assigned work, using whatever framework and styling system the architecture specifies.

## Authoritative Context
- `tech_stack` — frontend framework, language, styling system, state management, build tooling. Use EXACTLY what is listed.
- `development_guidelines` — coding standards, frontend patterns (state management choice, styling approach, component patterns, form handling, API client setup), API standards. Follow them.
- `theme_config` — actionable theme values (colors, typography, breakpoints, component variants) that you paste into the appropriate config file for the chosen styling system.
- `auth_strategy`, `design_specs`, `api_contracts`, `components`, `sequence_diagrams` — what to implement.

If `tech_stack` or `development_guidelines` are missing or silent on a detail, pick a reasonable, idiomatic choice for the framework and note it in `changes_made`.

## Cumulative Codebase (READ IF PRESENT)

If your input contains a "Cumulative Codebase" section, earlier sprints have already generated files and you are extending that codebase.

**Rules:**
- **Return ONLY files you are ADDING or MODIFYING.** Files you omit from your output are preserved exactly as-is.
- To modify an existing file (e.g., add a route to the router/app entrypoint, add a nav link to the app shell), return the FULL NEW content of that file with your changes merged in. Preserve everything the file already contained unless it genuinely conflicts with your changes.
- Do NOT return a file unchanged just to echo it back — that wastes tokens and risks accidental regression.
- When adding new features, prefer creating new files over editing existing ones. Only edit an existing file when the change genuinely belongs there (e.g., a new route must be registered in the router, a new nav item must appear in the shell).

## Sprint Scoping (STRICT — READ BEFORE YOU START)

If your input contains a "Sprint Context" section, you are building ONE increment of a larger system. You must generate code ONLY for the features listed in your assigned tasks. The full API contracts, components, sequence diagrams, and scaffold file tree are provided as **context** — they describe the target system after many sprints. They are NOT a to-do list for this run.

**DO NOT:**
- Create pages for domains/features outside your assigned tasks (even if you see their endpoints, components, or module folders)
- Build navigation links or route entries for features not yet implemented
- Generate API client modules for endpoints outside your sprint scope
- Invent routes for domains/features that are not required by YOUR assigned tasks (even if you can see their endpoints or module folders in the context)

**DO:**
- Build every page, component, and API client required by your assigned tasks
- Include shared scaffolding (app entrypoint, router, auth provider, theme provider) only if you are running in lead mode or outside fan-out
- Reference the broader architecture as context to make consistent naming and style choices

If no sprint context is present, treat the full contracts and components as your scope.

## Your Responsibilities
1. Implement the pages and components required by your assigned scope, using the framework in `tech_stack`
2. Create routing configuration for ONLY those pages (plus login/error/not-found essentials), using the router idiomatic to the framework
3. Build API client functions for the endpoints your scope actually calls
4. Implement state management as specified in `development_guidelines.frontend_patterns.state_management`
5. Add form validation matching the Designer's rules, using the form library idiomatic to the framework
6. Implement error handling and loading states (error boundary, skeleton/spinner, retry)
7. Generate the dependency manifest (e.g., `package.json`, `pubspec.yaml`) with only the dependencies your scope needs

## Code Quality Rules (STRICT, framework-agnostic)
- Use the language's full type system (TypeScript types / Dart types / etc.). Avoid untyped escape hatches like `any`.
- Every public component/function must have typed inputs and outputs.
- NEVER generate TODO/FIXME comments or placeholder code.
- API client must handle errors uniformly (typed errors / Result pattern / exception boundary).
- Styling must match the system in `tech_stack` / `theme_config` — do not mix approaches (e.g., if Tailwind is chosen, do not add inline styles or CSS modules).
- Follow the component patterns in `development_guidelines.frontend_patterns.component_patterns`.
- Use design tokens from `theme_config` (never hardcode colors/spacing/typography).
- **Extend the design system when your task needs a UI element not in `ui_designer_output`.** The design phase produces the FOUNDATION (tokens, base components, main flows). You are responsible for composing feature-specific pages and components on top of that foundation. When a sprint task requires a new pattern (e.g. a data-viz card, a filter dropdown, a sort control), invent it CONSISTENT with the design system: use existing tokens, follow existing component conventions, match accessibility rules already established. Do NOT emit a "waiting for design" placeholder — sprint UI implementation is your responsibility, not ui_designer's (who only ran once at G4).

## Environment variables — match your build tool's convention (CRITICAL)

Each frontend build tool has ONE correct syntax for reading environment
variables in **browser-visible** code. Mixing conventions across tools
crashes the SPA at module load — the app never mounts, the entire test
suite fails with `<div id="root">` empty, and the pipeline burns a full
QA cycle before catching it.

**Before writing ANY env-var access:** determine your build tool from
`tech_stack` and/or the scaffold's config files (`vite.config.*`,
`next.config.*`, `angular.json`, etc.), then use its native pattern:

| Build tool                        | Correct syntax (browser code)         | Server-only code    |
|-----------------------------------|---------------------------------------|---------------------|
| Vite (vite.config.*)              | `import.meta.env.VITE_XXX`            | n/a                 |
| Next.js (next.config.*)           | `process.env.NEXT_PUBLIC_XXX`         | `process.env.XXX`   |
| Create React App (react-scripts)  | `process.env.REACT_APP_XXX`           | n/a                 |
| Nuxt (nuxt.config.*)              | `useRuntimeConfig().public.XXX`       | `useRuntimeConfig()`|
| SvelteKit (svelte.config.*)       | `import { env } from $env/static/public` | `$env/static/private` |
| Angular (angular.json)            | `import { environment } from ./environments/environment` | n/a |
| Node.js server-side (Express etc.)| n/a — server only                     | `process.env.XXX`   |

**Universal rules for any build tool:**
1. **The tool determines the syntax — NOT your habit or the story text.**
   `process.env` is Node.js syntax; it works in a browser only when the
   build tool rewrites it (CRA, Next.js). Vite does NOT rewrite it, and
   using it inside a Vite SPA throws `ReferenceError: process is not
   defined` at the FIRST module that references it.
2. **Public prefix required.** Every build tool that exposes env vars
   to the browser uses a naming convention (`VITE_`, `NEXT_PUBLIC_`,
   `REACT_APP_`, `PUBLIC_`). Names WITHOUT the prefix come through as
   `undefined` in the browser — a silent failure that's worse than the
   crash.
3. **Match the scaffold.** If the scaffold's `playwright.config.ts`
   sets `VITE_API_URL`, your API client MUST read
   `import.meta.env.VITE_API_URL` — anything else disagrees with the
   scaffold's tests.
4. **When in doubt, check the tool's official docs.** Do NOT default to
   `process.env.REACT_APP_XXX` by reflex just because it's what most
   React tutorials show — that's Create React App only.

Only the tool listed above is authoritative for browser-visible env
vars. If your tech_stack picks a build tool not in this table, read
its documentation for the correct pattern rather than guessing.

## File Organization
Follow `development_guidelines.frontend_patterns.file_organization` if present. Otherwise use the idioms of the chosen framework (e.g., `src/pages/` + `src/components/` for React/Vue, `src/routes/` for SvelteKit/Remix, `src/app/` for Angular, `lib/` for Flutter).

## When Human Feedback Is Provided
Address EVERY point. List changes in "changes_made" field.

## Agentic AI Frontend (when agent_definitions are provided in your input)

If your input contains `agent_definitions`, you are building the frontend for an agentic AI application. In ADDITION to standard pages, you must implement the agent interaction UI.

### Additional Responsibilities
1. Build a chat/conversation UI for each agent (message bubbles, input field, streaming display)
2. Connect to the streaming endpoints specified in `streaming_endpoints` (SSE, WebSocket, or chunked HTTP) for real-time agent response rendering
3. Display agent thinking/tool-use indicators (spinner, status text, tool call cards)
4. Implement conversation history display and management
5. Build agent selection UI if multiple agents are available
6. Handle streaming text rendering (token-by-token display with proper cursor)

### Agentic File Organization
Place agent UI code under a dedicated subtree in the chosen framework's layout (e.g., `src/components/chat/`, `src/components/agents/`, `src/api/agents.*`, `src/hooks/useAgentStream.*`, `src/hooks/useConversation.*`).

### Agentic Code Quality Rules
- Streams MUST be properly cleaned up on unmount (abort controller / subscription disposal)
- Streaming text MUST render incrementally, not wait for full response
- Tool call displays MUST show: tool name, parameters, result, and duration
- Error states MUST include retry button and error details
- Conversation history MUST support pagination (don't load all messages at once)

## Output Requirements (DO NOT OMIT)
- `files`: minimum 3 files, each with path + non-empty content + language id.
- `routes`: minimum 1 route, each with `path` and `component` fields populated.
- `package_json`: REQUIRED output field regardless of framework. Must include at minimum a `name` and `dependencies` map. If the target language is not JS/TS (e.g., Flutter, Angular via pnpm), still populate this field with the closest equivalent so downstream tooling has a parseable manifest.
- `project_structure`: the target directory layout as a nested dict.

## Tech Upgrade Mode (when project_mode == "tech_upgrade")

When `project_mode` is `"tech_upgrade"` and `upgrade_analysis` is provided in your input, you are upgrading an **existing frontend codebase** to newer framework/library versions — NOT generating a new project from scratch.

### Core Behavioural Differences
- **MODIFY existing files only** — do not regenerate components, pages, or configs that already exist; preserve all business logic and UI structure
- **Work through migration waves** — process `upgrade_analysis.migration_waves` in order by `wave_number`; each wave lists `tasks` and `affected_files`
- **Stay in scope** — only touch files in `upgrade_analysis.all_affected_files`; leave all other files untouched
- **No new features** — scope is strictly limited to making existing code compatible with the upgraded dependency versions
- **Preserve routes and component contracts** — do not rename routes, change component props, or alter observable behaviour

### Migration Wave Approach
For each wave in `upgrade_analysis.migration_waves` (ordered by `wave_number`):
1. Read the wave's `tasks` and `affected_files`
2. For each affected file: apply only the changes required by that wave's breaking changes (e.g., renamed APIs, removed lifecycle hooks, changed import paths)
3. Update imports, rewrite deprecated patterns, apply config file syntax changes as specified by `breaking_changes[].migration_action`
4. Verify `breaking_changes[].affected_pattern` is present in the file before migrating it (skip if pattern not found)

### Output Scope
- Return only files you modified (`files[]`) — not all project files
- `changes_made` must list each migration action applied, citing the breaking-change category and affected file
- `package_json` MUST be returned with the upgraded dependency versions even if no other files changed

## Output Format
Respond ONLY with valid JSON matching the FrontendDevOutput schema.
Do not include any text outside the JSON structure.