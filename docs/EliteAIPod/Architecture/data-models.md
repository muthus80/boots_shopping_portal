---
project_id: 494d3fc8-f8b6-4f1f-a01d-d8d14f21994a
stage: architecture
agent: solution_architect
deliverable: Data-Models
generated_at: "2026-07-07T05:22:08.774633+00:00"
---

# Data Models — Boots Shopping App

> Generated by Elite AI Pod's Solution Architect agent on 2026-07-07T05:22+00:00.

## User

Registered user account with bcrypt-hashed password
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `email` | VARCHAR(255) | no | — | UNIQUE, NOT NULL |
| `hashed_password` | VARCHAR(255) | no | — | NOT NULL |
| `email_verified` | BOOLEAN | no | false | NOT NULL |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |
| `updated_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- ix_users_email

**Relationships:**
- one_to_many → RefreshToken
- one_to_many → Cart
- one_to_many → Order
- one_to_many → Review

_Used by: /api/v1/auth/register, /api/v1/auth/login, /api/v1/account/orders_
## RefreshToken

Hashed refresh tokens for JWT session persistence and revocation
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `user_id` | UUID | no | — | NOT NULL, FK users.id ON DELETE CASCADE |
| `token_hash` | VARCHAR(255) | no | — | UNIQUE, NOT NULL |
| `expires_at` | TIMESTAMPTZ | no | — | NOT NULL |
| `revoked` | BOOLEAN | no | false | NOT NULL |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- ix_refresh_tokens_token_hash
- ix_refresh_tokens_user_id

**Relationships:**
- many_to_one → User

_Used by: /api/v1/auth/refresh, /api/v1/auth/logout_
## Category

Product category (e.g., Work Boots, Hiking Boots)
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `name` | VARCHAR(100) | no | — | NOT NULL |
| `slug` | VARCHAR(100) | no | — | UNIQUE, NOT NULL |

**Indexes:**
- ix_categories_slug

**Relationships:**
- one_to_many → Product

_Used by: /api/v1/categories, /api/v1/products_
## Product

Boot product with images, attributes, and variants
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `category_id` | UUID | no | — | NOT NULL, FK categories.id |
| `name` | VARCHAR(255) | no | — | NOT NULL |
| `brand` | VARCHAR(100) | no | — | NOT NULL |
| `description` | TEXT | no | — | NOT NULL |
| `price` | NUMERIC(10,2) | no | — | NOT NULL |
| `images` | JSONB | no | '[]' | NOT NULL |
| `attributes` | JSONB | no | '{}' | NOT NULL |
| `search_vector` | TSVECTOR | yes | — | — |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- ix_products_category_id
- ix_products_search_vector

**Relationships:**
- many_to_one → Category
- one_to_many → ProductVariant
- one_to_many → Review

_Used by: /api/v1/products, /api/v1/products/search, /api/v1/products/{product_id}_
## ProductVariant

Size/color variant of a product with inventory count
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `product_id` | UUID | no | — | NOT NULL, FK products.id ON DELETE CASCADE |
| `size` | VARCHAR(20) | no | — | NOT NULL |
| `color` | VARCHAR(50) | no | — | NOT NULL |
| `inventory_count` | INTEGER | no | 0 | NOT NULL, CHECK (inventory_count >= 0) |

**Indexes:**
- ix_product_variants_product_id
- ix_product_variants_size
- ix_product_variants_color

**Relationships:**
- many_to_one → Product
- one_to_many → CartItem
- one_to_many → OrderItem

_Used by: /api/v1/products/{product_id}, /api/v1/cart/items_
## Cart

Shopping cart supporting both guest (session-based) and authenticated users
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `user_id` | UUID | yes | — | FK users.id ON DELETE SET NULL |
| `session_id` | VARCHAR(255) | yes | — | — |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |
| `updated_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- ix_carts_user_id
- ix_carts_session_id

**Relationships:**
- many_to_one → User
- one_to_many → CartItem

_Used by: /api/v1/cart, /api/v1/cart/items_
## CartItem

Individual line item in a shopping cart
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `cart_id` | UUID | no | — | NOT NULL, FK carts.id ON DELETE CASCADE |
| `product_variant_id` | UUID | no | — | NOT NULL, FK product_variants.id |
| `quantity` | INTEGER | no | 1 | NOT NULL, CHECK (quantity >= 1) |

**Indexes:**
- ix_cart_items_cart_id

**Relationships:**
- many_to_one → Cart
- many_to_one → ProductVariant

_Used by: /api/v1/cart, /api/v1/cart/items, /api/v1/cart/items/{item_id}_
## Order

Completed purchase order for guest or authenticated user
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `order_number` | VARCHAR(50) | no | — | UNIQUE, NOT NULL |
| `user_id` | UUID | yes | — | FK users.id ON DELETE SET NULL |
| `guest_email` | VARCHAR(255) | yes | — | — |
| `status` | VARCHAR(50) | no | 'pending' | NOT NULL |
| `total_amount` | NUMERIC(10,2) | no | — | NOT NULL |
| `shipping_address` | JSONB | no | '{}' | NOT NULL |
| `stripe_payment_intent_id` | VARCHAR(255) | yes | — | — |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- ix_orders_user_id
- ix_orders_order_number

**Relationships:**
- many_to_one → User
- one_to_many → OrderItem
- one_to_many → Review

_Used by: /api/v1/checkout/confirm, /api/v1/account/orders_
## OrderItem

Individual line item in an order with captured unit price
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `order_id` | UUID | no | — | NOT NULL, FK orders.id ON DELETE CASCADE |
| `product_variant_id` | UUID | no | — | NOT NULL, FK product_variants.id |
| `quantity` | INTEGER | no | — | NOT NULL |
| `unit_price` | NUMERIC(10,2) | no | — | NOT NULL |

**Indexes:**
- ix_order_items_order_id

**Relationships:**
- many_to_one → Order
- many_to_one → ProductVariant

_Used by: /api/v1/checkout/confirm, /api/v1/account/orders_
## Review

Purchase-verified product review linking product, user, and verifying order
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `product_id` | UUID | no | — | NOT NULL, FK products.id ON DELETE CASCADE |
| `user_id` | UUID | no | — | NOT NULL, FK users.id ON DELETE CASCADE |
| `order_id` | UUID | no | — | NOT NULL, FK orders.id |
| `rating` | SMALLINT | no | — | NOT NULL, CHECK (rating >= 1 AND rating <= 5) |
| `review_text` | TEXT | no | — | NOT NULL |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- ix_reviews_product_id
- ix_reviews_user_id_product_id

**Relationships:**
- many_to_one → Product
- many_to_one → User
- many_to_one → Order

_Used by: /api/v1/products/{product_id}/reviews_
