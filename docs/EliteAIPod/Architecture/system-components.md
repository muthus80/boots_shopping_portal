---
project_id: 494d3fc8-f8b6-4f1f-a01d-d8d14f21994a
stage: architecture
agent: solution_architect
deliverable: System-Components
generated_at: "2026-07-07T05:22:07.243372+00:00"
---

# System Components — Boots Shopping App

> Generated by Elite AI Pod's Solution Architect agent on 2026-07-07T05:22+00:00.

## ReactFrontend (service)

React 18 single-page application served as static files via Nginx. Handles all UI rendering, client-side routing, form validation, cart state, and WCAG 2.1 AA accessibility.

**Responsibilities:**
- Render product listing, search, and detail pages
- Manage shopping cart UI state with optimistic updates
- Render checkout flow with Stripe Elements integration
- Handle JWT storage, session persistence, and token refresh
- Display loading spinners, empty states, and error banners
- Ensure keyboard navigability and ARIA compliance (WCAG 2.1 AA)
**Depends on:** FastAPIServer**Communication protocol:** HTTP/HTTPS REST
**Port:** 3000
## NginxProxy (gateway)

Nginx reverse proxy that routes API requests to FastAPI and serves React static assets. Handles TLS termination and sets cache headers for static resources.

**Responsibilities:**
- Route /api/* requests to FastAPI backend
- Serve React SPA static build for all non-API routes
- Terminate TLS and enforce HTTPS
- Set cache-control headers for product images and static assets
**Depends on:** FastAPIServer**Communication protocol:** HTTP
**Port:** 80
## FastAPIServer (service)

Python FastAPI application implementing all REST API endpoints organized into domain modules: auth, products, cart, checkout, reviews, and account.

**Responsibilities:**
- Implement authentication and JWT issuance/refresh (auth module)
- Serve product catalog, search, and filter endpoints (products module)
- Manage cart CRUD operations for guest and authenticated users (cart module)
- Process checkout: create Stripe PaymentIntent and confirm orders (checkout module)
- Serve and accept purchase-verified product reviews (reviews module)
- Return authenticated user order history (account module)
**Depends on:** PostgreSQLDatabase, StripeGateway, EmailService**Communication protocol:** HTTP REST
**Port:** 8000
## PostgreSQLDatabase (database)

PostgreSQL 16 relational database storing all application data: users, products, categories, variants, carts, orders, reviews, and refresh tokens.

**Responsibilities:**
- Persist user accounts and hashed passwords
- Store product catalog, categories, and variants with inventory
- Maintain cart and order records with ACID transaction guarantees
- Store purchase-verified reviews with order_id foreign key
- Provide full-text search via tsvector indexes on product name and description
**Communication protocol:** PostgreSQL wire protocol
**Port:** 5432
## StripeGateway (gateway)

Stripe payment processing integration. FastAPI creates PaymentIntents server-side; the React frontend uses Stripe Elements for PCI-compliant card capture. No card data touches application servers.

**Responsibilities:**
- Create PaymentIntents server-side with order total and currency
- Verify payment success before creating order records
- Provide client_secret to frontend for Stripe Elements card capture
**Communication protocol:** HTTPS REST

## EmailService (service)

Transactional email provider (e.g., SendGrid or Postmark) used for registration confirmation and order confirmation emails.

**Responsibilities:**
- Send email confirmation on user registration (US-001)
- Send order confirmation email after successful checkout (US-011)
**Communication protocol:** HTTPS REST

